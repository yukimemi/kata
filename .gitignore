# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# kata's own state
# Phase 1 dogfood: `.kata/applied.toml` records the local preset
# path the author ran `kata init` with — not portable across
# machines yet. Tracked-status returns once Phase 2 ships
# git-fetched templates and the portability follow-up in
# ROADMAP "Phase 1 follow-ups".
/.kata/

# Editor
.idea/
.vscode/
*.swp
*~

# OS
.DS_Store
Thumbs.db

# APM compiled outputs (committed for visibility once apm.yml exists)
# Uncomment if we change strategy:
# /.claude/skills/
# /.gemini/skills/
# /.github/skills/
