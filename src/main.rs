// Phase 1 will replace this with the real CLI dispatch.
// Kept as a placeholder so `cargo check` succeeds during the
// design-only window between repo creation and Phase 1 start.

fn main() {
    eprintln!("kata: not yet implemented. See ROADMAP.md.");
    std::process::exit(2);
}
